# 开发变更流程

> 状态：Current  
> 最后验证：2026-08-08

1. 先定位：阅读 Current 文档、搜索代码、迁移与测试，确认真实入口。
2. 先定规格：符合 [Spec 规则](../specs/README.md) 的变更先写 Spec；架构取舍先写 ADR。
3. 再实施：控制改动范围，保留权限、迁移和兼容边界。
4. 验证：运行与改动相称的测试、构建和静态检查；检查前后端与迁移协作。
5. 收尾：更新 Current 文档和文档索引；将完成的 Spec 标记为 Implemented，检查链接、`git diff` 与 `git status`。
